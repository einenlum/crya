# Uncomment and configure to enable CORS.
# If this file does not exist, no CORS headers are added.
#
# config = {
#     "paths": ["/api/*"],
#     "allowed_origins": ["https://example.com"],
#     "allowed_methods": ["GET", "POST", "PUT", "DELETE"],
#     "allowed_headers": ["*"],
#     "supports_credentials": False,
# }
