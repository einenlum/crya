# Configure CORS. All fields are optional — defaults are used for any omitted key.

config = {
    # "paths": ["/api/*"],
    # "allowed_origins": ["*"],
    # "allowed_methods": ["*"],
    # "allowed_headers": ["*"],
    # "supports_credentials": False,
}
