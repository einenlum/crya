[project]
name = "crya-skeleton"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "crya",
]

[tool.uv.sources]
crya = { path = "../crya-framework" }

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
